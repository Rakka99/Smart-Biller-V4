import { Router } from "express";
import { auth } from "../middleware/auth";
import { prisma } from "../lib/prisma";
const router=Router(); router.use(auth);
router.get("/",async(req,res,next)=>{try{const regionId=typeof req.query.regionId==="string"?req.query.regionId:undefined;const ulpId=typeof req.query.ulpId==="string"?req.query.ulpId:undefined;const from=typeof req.query.from==="string"?new Date(req.query.from):new Date(new Date().getFullYear(),new Date().getMonth(),1);const to=typeof req.query.to==="string"?new Date(req.query.to):new Date();const users=await prisma.user.findMany({where:{active:true,role:"BILLER",...(regionId?{regionId}:{}),...(ulpId?{ulpId}:{})},include:{region:true,ulp:true}});const rows=[];for(const user of users){const assignments=await prisma.customerAssignment.findMany({where:{userId:user.id,active:true},select:{customerId:true}});const payments=await prisma.payment.findMany({where:{userId:user.id,status:"SUCCESS",createdAt:{gte:from,lte:to}},select:{customerId:true,period:true,sellingPrice:true}});const uniqueCustomers=new Set(payments.map(p=>p.customerId)).size;const periods=payments.length;const amount=payments.reduce((sum,p)=>sum+(p.sellingPrice??0),0);rows.push({userId:user.id,name:user.name,region:user.region?.name,ulp:user.ulp?.name,uniqueCustomers,periods,amount,assignedCustomers:assignments.length});}rows.sort((a,b)=>b.uniqueCustomers-a.uniqueCustomers||b.periods-a.periods||b.amount-a.amount);res.json({metric:"unique_customers",rows});}catch(e){next(e)}});
export default router;
